<h1>Editing list</h1>

<% form_for([@user, @list]) do |f| %>
  <%= f.error_messages %>

 <%= render :partial => "form", :locals => { :f => f } %>

<% end %>

<%= link_to 'Show', [@user, @list] %> |
<%= link_to 'Back', user_lists_path(@user) %>
